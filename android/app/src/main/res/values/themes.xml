<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.TravelBoard" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/amber</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/slate950</item>
    </style>

    <!-- No-UI theme for the share router activity. -->
    <style name="Theme.TravelBoard.Transparent" parent="Theme.Material3.DayNight">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>
