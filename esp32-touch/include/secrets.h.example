#pragma once

/** Copy to secrets.h and edit (secrets.h is gitignored). */

#define TB_WIFI_SSID "YOUR_WIFI_SSID"
#define TB_WIFI_PASS "YOUR_WIFI_PASSWORD"
#define TB_API_BASE "http://10.0.0.73:3000"

/** 0=WiFi only, 1=USB only (run npm run esp32:usb-bridge), 2=auto */
#define TB_DATA_TRANSPORT 2
